{
  "$schema": "https://schema.tauri.app/config/2.0",
  "identifier": "default",
  "description": "Default permissions for rats app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "fs:allow-read-file",
    "fs:allow-read-dir",
    "fs:allow-exists",
    "shell:allow-open"
  ]
}
